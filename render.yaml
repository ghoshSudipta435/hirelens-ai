services:
  - type: web
    name: hirelens-ai-backend
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install --include=dev && npm run db:generate && npm run build --workspace backend
    startCommand: npm run start --workspace backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
